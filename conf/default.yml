app: viper-default
mysql:
  uri: root:@tcp(localhost:3306)/blog?parseTime=true
  # 设置连接池中的最大闲置连接数
  maxOpenConns: 20
  # 设置连接池中的最大闲置连接数
  maxIdleConns: 2

log:
redis:
  mode: single
  conn: 127.0.0.1:6379
  password:
  conn_timeout: 2000
  so_timeout: 4000
  min_idle_conns: 10
  max_retries: 3
  sentinel:
    master:
prefix: http://mv-res.xdplt.com/


horizontal: {"spxitongdogcom": "true","h5xyuheixiacom": "true","spsharepickdokicn":"true"}
